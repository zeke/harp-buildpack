#!/usr/bin/env bash
# bin/detect <build-dir>

if [ -f $1/harp.json ] || [ -f $1/index.ejs ] || [ -f $2/index.jade ] || [ -f $2/index.md ] || [ -f $1/index.html ]; then
  echo "Harp.js static" && exit 0
else
  echo "no" && exit 1
fi
